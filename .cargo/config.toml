# Cargo configuration

[build]
# Use more aggressive optimization for fuzzing
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1

# Dedicated profile for fuzzing
[profile.fuzz]
inherits = "dev"
opt-level = 3
incremental = false
codegen-units = 1
